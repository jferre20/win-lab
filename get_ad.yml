---
- hosts: ad01
  gather_facts: false
  tasks:
    - name: Executar script que já existe no servidor Windows
      ansible.windows.win_shell: powershell.exe -ExecutionPolicy Bypass -File "C:\Users\joaferre\script_shell.ps1"
      register: script_result

    - name: Ver o resultado do script na tela
      ansible.builtin.debug:
        var: script_result.stdout
